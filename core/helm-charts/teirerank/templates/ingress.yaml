# Copyright (C) 2025-2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0
{{- if .Values.ingress.enabled }}
{{- $modelName := .Values.RERANK_MODEL_ID | splitList "/" | last }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  labels:
    {{- include "teirerank.labels" . | nindent 4 }}
  name: {{ include "teirerank.fullname" . }}-httproute
  namespace: {{- if .Values.apisix.enabled }}
                auth-apisix
              {{- else }}
                default
              {{- end }}
spec:
  parentRefs:
    - name: enterprise-edge-gateway
      namespace: envoy-gateway-system
  hostnames:
    - {{ .Values.ingress.host }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{- if and .Values.apisix.enabled .Values.accelDevice }}
              /{{ $modelName }}
              {{- else if and .Values.apisix.enabled (not .Values.accelDevice) }}
              /{{ $modelName }}-teirerankcpu
              {{- else }}
              /{{ $modelName }}
              {{- end }}
      {{- if not .Values.apisix.enabled }}
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      {{- end }}
      backendRefs:
        - name: {{- if .Values.apisix.enabled }}
            auth-apisix-gateway
          {{- else }}
            {{ include "teirerank.fullname" . }}-service
          {{- end }}
          port: 80
{{- end }}