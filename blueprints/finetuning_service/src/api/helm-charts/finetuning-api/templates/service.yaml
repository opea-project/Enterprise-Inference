apiVersion: v1
kind: Service
metadata:
  name: {{ include "finetuning-service.fullname" . }}
  labels:
    {{- include "finetuning-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.app.service.type }}
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finetuning-service.selectorLabels" . | nindent 4 }}
