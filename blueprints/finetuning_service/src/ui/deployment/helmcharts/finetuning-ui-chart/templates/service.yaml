apiVersion: v1
kind: Service
metadata:
  name: {{ include "finetuning-ui.fullname" . }}
  labels:
    {{- include "finetuning-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finetuning-ui.selectorLabels" . | nindent 4 }}
